import React from 'react'
import app from './app.jsx'
import { shallow } from 'enzyme'

describe('Description of the this whole page of test', ()=>{
  test('Make sure that for an input of 7 the answer is 14', () => {
    const fakeInput = 7
    const fakeOutput = 14

  })
})
